MyPim
=====


Code Style
==========

The base code follows slightly different style than the GUI.  Base code uses
function names that are lowercase with underscore.  The GUI code follows a style
similar to the GUI toolkit used.


